#include<stdio.h>
int main(){
    int a,square,i;
    printf("Input number of terms: ");
    scanf("%d",&a);

    while(i<=a){;
    printf("\nNumber is : %d and Square of the %d is :%d",a, a, a*a);
    i++;
    }
    return 0;
}